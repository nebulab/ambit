# Build output and caches — generated, and reformatting them would only churn.
dist/
release/
coverage/
*.tsbuildinfo

# Fixture and scratch trees written by the suite at runtime.
test/tmp/

# Recorded program output, compared byte-for-byte against what ambit emits. Formatting these would
# assert Prettier's JSON style rather than ambit's, and the goldens would fail the moment they were
# written. They are regenerated with UPDATE_GOLDEN=1, never by hand.
test/golden/

# Bun owns this file's formatting; `bun install` rewrites it on its own terms.
bun.lock

# Agent-harness state, not source.
.claude/
